import { Meta, Title, Subtitle, Description, Primary, Controls } from '@storybook/blocks';
import { Button } from '@matcha-ui/components';

<Meta title="Docs/Introduction" />

# Matcha UI

This is the documentation for **Matcha UI**.

## Getting started

Install the package:

```bash
pnpm add @matcha-ui/components
```

Import styles once (explicit import by design):

```ts
import '@matcha-ui/components/styles.css';
```

Use a component:

<Button>Button</Button>
