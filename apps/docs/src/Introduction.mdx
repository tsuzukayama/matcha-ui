import { Meta, Title, Subtitle, Description, Primary, Controls } from '@storybook/blocks';
import { Button } from '@suzuka-ds/components';

<Meta title="Docs/Introduction" />

# Suzuka DS

This is the documentation for **Suzuka Design System**.

## Getting started

Install the package:

```bash
pnpm add @suzuka-ds/components
```

Import styles once (explicit import by design):

```ts
import '@suzuka-ds/components/styles.css';
```

Use a component:

<Button>Button</Button>


